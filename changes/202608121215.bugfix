:recycle: `[value]` Align `StringConverter` with flatten-map scalar formatting while preferring `fmt.Stringer` and `encoding.TextMarshaler`, wrapping text-marshalling failures as `commonerrors.ErrMarshalling`, and documenting pointer and struct fallback output.
