:recycle: `[validation]` Time validation now supports optional `time.Duration` and `time.Time` pointers consistently, accepts RFC3339 timestamp strings and optional string pointers, and adds a `NilTimestampOrNotEmpty` helper for optional timestamp fields.
