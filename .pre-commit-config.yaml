- repo: local
  hooks:
    - id: goimports
      name: goimports
      description: "Runs goimports, requires golang"
      entry: goimports -l -w -e -local 'github.com/ARMmbed'
      language: system
      types: [go]
      require_serial: true
